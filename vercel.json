{
  "builds": [
    {
      "src": "app/main.py",
      "use": "@python",
      "command": "pip install -r requirements.txt && python app/main.py"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/app/main.py" }
  ],
  "env": {
    "UVICORN_PORT": "8080"
  }
}
